# Robotics Landing

Lightweight 3D landing page demonstrating a simple robot animation using Three.js.

How to run:

1. Open index.html in a browser (or serve via static file server).
2. Interact with the scene using mouse or touch. 

Notes:
- Uses Three.js from CDN for simplicity.
- No external glTF models included to keep the demo lightweight.
